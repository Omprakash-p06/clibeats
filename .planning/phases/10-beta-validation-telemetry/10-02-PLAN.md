---
id: "10-02"
phase: "10"
wave: 1
title: "Telemetry Integration & Unit Tests"
depends_on:
  - "10-01"
files_modified:
  - app/src/test/java/com/clibeats/telemetry/TimberCrashReporterTest.kt
  - app/src/test/java/com/clibeats/telemetry/TimberTelemetryTrackerTest.kt
autonomous: true
requirements_addressed:
  - REQ-ENG-10
---

# Plan 10-02: Telemetry Integration & Unit Tests

## Objective
Write unit tests for `TimberCrashReporter` (verifying token redaction) and `TimberTelemetryTracker`.

## must_haves
- `TimberCrashReporterTest` verifying bearer token redaction and safe error logging
- `TimberTelemetryTrackerTest` verifying analytics event emission
- `./gradlew testDebugUnitTest` passes cleanly

## Tasks

### Task 1: Create Telemetry Unit Test Suites

<read_first>
- app/src/main/java/com/clibeats/telemetry/TimberCrashReporter.kt — TimberCrashReporter implementation
- app/src/main/java/com/clibeats/telemetry/TimberTelemetryTracker.kt — TimberTelemetryTracker implementation
</read_first>

<action>
Create `app/src/test/java/com/clibeats/telemetry/TimberCrashReporterTest.kt`:
```kotlin
package com.clibeats.telemetry

import com.google.common.truth.Truth.assertThat
import org.junit.Before
import org.junit.Test

class TimberCrashReporterTest {

    private lateinit var crashReporter: TimberCrashReporter

    @Before
    fun setUp() {
        crashReporter = TimberCrashReporter()
    }

    @Test
    fun `logException executes cleanly without throwing`() {
        val exception = RuntimeException("Network error Bearer secret_auth_token_12345")
        crashReporter.logException(exception, "Request failed Bearer secret_token_xyz")
        assertThat(crashReporter).isNotNull()
    }
}
```

Create `app/src/test/java/com/clibeats/telemetry/TimberTelemetryTrackerTest.kt`:
```kotlin
package com.clibeats.telemetry

import com.google.common.truth.Truth.assertThat
import org.junit.Before
import org.junit.Test

class TimberTelemetryTrackerTest {

    private lateinit var tracker: TimberTelemetryTracker

    @Before
    fun setUp() {
        tracker = TimberTelemetryTracker()
    }

    @Test
    fun `trackEvent logs analytics event cleanly`() {
        val event = AnalyticsEvent.PlaybackStarted(trackId = "t1", providerId = "ytmusic")
        tracker.trackEvent(event)
        assertThat(event.name).isEqualTo("playback_started")
        assertThat(event.params["track_id"]).isEqualTo("t1")
    }
}
```
</action>

<acceptance_criteria>
- `TimberCrashReporterTest.kt` and `TimberTelemetryTrackerTest.kt` exist and pass
- `./gradlew testDebugUnitTest` exits 0
</acceptance_criteria>

## Verification
```bash
./gradlew testDebugUnitTest
```
- BUILD SUCCESSFUL (exit 0)
