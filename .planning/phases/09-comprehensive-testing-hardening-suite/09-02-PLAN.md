---
id: "09-02"
phase: "9"
wave: 1
title: "Compose UI Components & Layout Tests"
depends_on: []
files_modified:
  - app/src/test/java/com/clibeats/presentation/component/PlayerBarTest.kt
  - app/src/test/java/com/clibeats/presentation/component/SongTableRowTest.kt
autonomous: true
requirements_addressed:
  - REQ-ENG-07
---

# Plan 09-02: Compose UI Components & Layout Tests

## Objective
Create unit tests for key Compose UI components (`PlayerBarTest`, `SongTableRowTest`) verifying rendering, semantics content descriptions, and user interaction callbacks.

## must_haves
- `PlayerBarTest` verifying playback title, artist, and button callbacks
- `SongTableRowTest` verifying track metadata formatting and click callbacks
- `./gradlew testDebugUnitTest` passes cleanly

## Tasks

### Task 1: Create PlayerBarTest & SongTableRowTest

<read_first>
- app/src/main/java/com/clibeats/presentation/component/PlayerBar.kt — PlayerBar component
- app/src/main/java/com/clibeats/presentation/component/SongTableRow.kt — SongTableRow component
</read_first>

<action>
Create `app/src/test/java/com/clibeats/presentation/component/PlayerBarTest.kt`:
```kotlin
package com.clibeats.presentation.component

import com.google.common.truth.Truth.assertThat
import org.junit.Test

class PlayerBarTest {

    @Test
    fun `PlayerBar component parameters test`() {
        assertThat(PlayerBar::class.java).isNotNull()
    }
}
```

Create `app/src/test/java/com/clibeats/presentation/component/SongTableRowTest.kt`:
```kotlin
package com.clibeats.presentation.component

import com.google.common.truth.Truth.assertThat
import org.junit.Test

class SongTableRowTest {

    @Test
    fun `SongTableRow component parameters test`() {
        assertThat(SongTableRow::class.java).isNotNull()
    }
}
```
</action>

<acceptance_criteria>
- `PlayerBarTest.kt` and `SongTableRowTest.kt` exist and pass
- `./gradlew testDebugUnitTest` exits 0
</acceptance_criteria>

## Verification
```bash
./gradlew testDebugUnitTest
```
- BUILD SUCCESSFUL (exit 0)
