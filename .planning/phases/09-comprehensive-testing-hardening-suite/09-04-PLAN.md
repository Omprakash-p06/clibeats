---
id: "09-04"
phase: "9"
wave: 3
title: "Static Analysis Hardening, ADR-009 & Quality Gate"
depends_on:
  - "09-03"
files_modified:
  - .github/workflows/ci.yml
  - docs/adr/ADR-009-comprehensive-testing-hardening.md
autonomous: true
requirements_addressed:
  - REQ-ENG-01
  - REQ-ENG-06
  - REQ-ENG-07
---

# Plan 09-04: Static Analysis Hardening, ADR-009 & Quality Gate

## Objective
Verify CI GitHub Actions workflow file `.github/workflows/ci.yml` is fully synchronized with project quality gates. Create `ADR-009` documenting testing strategy and coverage standards. Execute full quality gate (`assembleDebug`, `testDebugUnitTest`, `ktlintCheck`, `detekt`).

## must_haves
- `.github/workflows/ci.yml` synchronized with project quality gate steps (`assembleDebug`, `testDebugUnitTest`, `ktlintCheck`, `detekt`)
- `ADR-009-comprehensive-testing-hardening.md` written to `docs/adr/`
- `./gradlew assembleDebug testDebugUnitTest ktlintCheck detekt` all exit 0 with 0 errors

## Tasks

### Task 1: Verify & Sync CI Workflow File .github/workflows/ci.yml

<read_first>
- .github/workflows/ci.yml — GitHub Actions workflow configuration
</read_first>

<action>
Inspect and verify `.github/workflows/ci.yml` contains:
```yaml
name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build & Run Quality Gates
      run: ./gradlew assembleDebug testDebugUnitTest ktlintCheck detekt --continue
```
</action>

<acceptance_criteria>
- `.github/workflows/ci.yml` updated and synchronized
</acceptance_criteria>

---

### Task 2: Write ADR-009

<action>
Create `docs/adr/ADR-009-comprehensive-testing-hardening.md`:
```markdown
# ADR-009: Comprehensive Testing & Hardening Architecture

**Date:** 2026-08-06
**Status:** Accepted
**Phase:** 9 — Comprehensive Testing & Hardening Suite

## Context

CLIBeats requires >=85% unit/integration coverage (`REQ-ENG-01`), zero static analysis issues (`REQ-ENG-06`), and automated Compose UI component tests (`REQ-ENG-07`).

## Decision

### 1. Multi-Tiered Testing Suite
- Domain & Data Layer: Unit tests for all Repositories, DAOs, Interceptors, and Mappers.
- Presentation Layer: Unit tests for 100% of ViewModels (`PlayerViewModel`, `SearchViewModel`, `QueueViewModel`, `LibraryViewModel`, `PlaylistViewModel`, `SettingsViewModel`).
- UI Components: Compose UI tests for `PlayerBar` and `SongTableRow`.
- Integration: `PlaybackIntegrationTest` verifying end-to-end user flows.

### 2. CI Quality Gate Enforcement
- `.github/workflows/ci.yml` enforces mandatory execution of `assembleDebug`, `testDebugUnitTest`, `ktlintCheck`, and `detekt` prior to merge.

## Consequences

### Positive
- Zero regression risk across core playback and database layers.
- Automated code formatting and static analysis compliance.
- Verified CI pipeline synchronization.
```
</action>

<acceptance_criteria>
- `docs/adr/ADR-009-comprehensive-testing-hardening.md` exists
</acceptance_criteria>

---

### Task 3: Full Quality Gate Check

<action>
Run full quality gate:
```bash
./gradlew testDebugUnitTest ktlintCheck detekt assembleDebug
```
Fix any formatting or detekt issues if reported.
</action>

<acceptance_criteria>
- `./gradlew testDebugUnitTest ktlintCheck detekt assembleDebug` exits 0
- All tests pass cleanly
</acceptance_criteria>

## Verification
```bash
./gradlew testDebugUnitTest ktlintCheck detekt assembleDebug
```
- BUILD SUCCESSFUL (exit 0)
