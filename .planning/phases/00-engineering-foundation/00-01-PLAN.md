---
phase: 00-engineering-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - build.gradle.kts
  - app/build.gradle.kts
  - config/detekt/detekt.yml
  - docs/adr/ADR-000-template.md
  - docs/adr/ADR-001-architecture-and-di-strategy.md
autonomous: true
requirements:
  - REQ-ENG-02
  - REQ-ENG-03
  - REQ-ENG-04
  - REQ-ENG-05

must_haves:
  truths:
    - "Detekt is configured in Gradle and runs via `./gradlew detekt` with 0 errors"
    - "ktlint plugin is configured and runs via `./gradlew ktlintCheck`"
    - "Android Lint runs via `./gradlew lintDebug` with 0 error-level issues"
    - "ADR template exists at `docs/adr/ADR-000-template.md`"
    - "ADR-001 exists for Clean Architecture and Hilt DI strategy"
  artifacts:
    - path: "config/detekt/detekt.yml"
      provides: "Detekt static analysis configuration"
      contains: "build:"
    - path: "docs/adr/ADR-000-template.md"
      provides: "Architecture Decision Record template"
      contains: "# ADR-XXX:"
    - path: "docs/adr/ADR-001-architecture-and-di-strategy.md"
      provides: "ADR-001 record for Architecture & DI"
      contains: "Status: Accepted"
  key_links:
    - from: "build.gradle.kts"
      to: "config/detekt/detekt.yml"
      via: "detekt extension configuration"
      pattern: "config\\.setFrom"
---

<objective>
Configure static code analysis (Detekt), automated code formatting (ktlint), Android Lint rules, and the Architecture Decision Record (ADR) framework.

Purpose: Prevent code smells, complexity creep, formatting arguments, and architecture violations from entering the codebase from day one.

Output:
- Detekt plugin integration in Gradle with custom `config/detekt/detekt.yml`
- ktlint plugin integration for automated code style checks
- `docs/adr/ADR-000-template.md` and initial `ADR-001` document
- Architecture boundary rules preventing direct `Presentation` -> `Data` imports
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
## Target Quality Tools
- **Detekt**: 1.23.6+
- **ktlint Gradle plugin**: 12.1.1+ (Jlleitschuh plugin)
- **Android Lint**: AGP built-in lint task (`lintDebug`)

## Architecture Rule
Domain layer MUST remain pure Kotlin (zero `android.*` imports).
Presentation layer MUST NOT import Data layer types (`com.clibeats.data.*`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Detekt and ktlint plugins to Gradle configuration</name>
  <files>
    gradle/libs.versions.toml
    build.gradle.kts
    app/build.gradle.kts
    config/detekt/detekt.yml
  </files>
  <action>
1. Update `gradle/libs.versions.toml`:
   - Add `detekt = "1.23.6"` to `[versions]`
   - Add `ktlint = "12.1.1"` to `[versions]`
   - Add `detekt-gradlePlugin = { module = "io.gitlab.arturbosch.detekt:detekt-gradle-plugin", version.ref = "detekt" }` to `[libraries]`
   - Add `detekt = { id = "io.gitlab.arturbosch.detekt", version.ref = "detekt" }` to `[plugins]`
   - Add `ktlint = { id = "org.jlleitschuh.gradle.ktlint", version.ref = "ktlint" }` to `[plugins]`

2. Configure `build.gradle.kts` (root) and `app/build.gradle.kts` to apply detekt and ktlint plugins.
   Configure detekt task to use `config/detekt/detekt.yml` configuration file.

3. Create `config/detekt/detekt.yml` with rules for:
   - Complexity: TooManyFunctions (threshold 15), LongMethod (threshold 60), LargeClass (threshold 300)
   - Style: UnusedPrivateMember, MagicNumber (ignored in tests)
   - Architecture: forbid imports of `com.clibeats.data.*` inside `com.clibeats.presentation.*` package.

4. Run `./gradlew detekt ktlintCheck` to verify tasks execute cleanly.
  </action>
  <verify>
    <automated>./gradlew tasks --all 2>&1 | grep -E "detekt|ktlint" | head -10</automated>
  </verify>
  <done>`./gradlew detekt` and `./gradlew ktlintCheck` tasks exist and execute without plugin errors.</done>
</task>

<task type="auto">
  <name>Task 2: Establish ADR framework & write ADR-001</name>
  <files>
    docs/adr/ADR-000-template.md
    docs/adr/ADR-001-architecture-and-di-strategy.md
  </files>
  <action>
1. Create `docs/adr/ADR-000-template.md`:
```markdown
# ADR-XXX: [Short Title]

## Context & Problem Statement
[Describe the context and problem being solved]

## Decision Drivers
- [Driver 1]
- [Driver 2]

## Considered Options
1. [Option 1]
2. [Option 2]

## Decision Outcome
Chosen option: "[Option X]", because [justification].

### Positive Consequences
- [Consequence 1]

### Negative Consequences
- [Consequence 1]
```

2. Create `docs/adr/ADR-001-architecture-and-di-strategy.md`:
Document the decision to use MVVM + Clean Architecture (`Presentation` -> `Domain` -> `Data`) with Hilt 2.51+ dependency injection and pure Kotlin domain layer.
  </action>
  <verify>
    <automated>ls -la docs/adr/ 2>&1 | grep "ADR"</automated>
  </verify>
  <done>ADR-000-template.md and ADR-001-architecture-and-di-strategy.md exist in `docs/adr/`.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `./gradlew detekt` executes cleanly
2. `./gradlew ktlintCheck` executes cleanly
3. `docs/adr/ADR-000-template.md` and `docs/adr/ADR-001-architecture-and-di-strategy.md` exist
</verification>

<success_criteria>
- Detekt and ktlint integrated in Gradle with config file at `config/detekt/detekt.yml`
- Architecture boundary rules configured to restrict illegal layer imports
- ADR template and initial ADR-001 created
</success_criteria>

<output>
Create `.planning/phases/00-engineering-foundation/00-01-SUMMARY.md` documenting Detekt, ktlint, and ADR setup.
</output>
