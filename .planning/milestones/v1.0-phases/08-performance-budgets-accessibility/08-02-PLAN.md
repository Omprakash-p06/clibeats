---
id: "08-02"
phase: "8"
wave: 1
title: "Cold Start & App Initialization Optimization"
depends_on:
  - "08-01"
files_modified:
  - app/src/main/java/com/clibeats/CliBeatsApplication.kt
autonomous: true
requirements_addressed:
  - REQ-NFR-01
---

# Plan 08-02: Cold Start & App Initialization Optimization

## Objective
Audit and optimize cold start initialization in `CliBeatsApplication.kt` and Hilt startup routines to guarantee startup overhead remains well within the <2s performance budget.

## must_haves
- Startup path in `CliBeatsApplication.kt` clean and minimal
- Heavy singletons lazy-loaded outside immediate UI launch path
- `./gradlew compileDebugKotlin assembleDebug` passes

## Tasks

### Task 1: Audit & Optimize CliBeatsApplication.kt Startup

<read_first>
- app/src/main/java/com/clibeats/CliBeatsApplication.kt — Application entry point
</read_first>

<action>
Review `app/src/main/java/com/clibeats/CliBeatsApplication.kt`:
```kotlin
package com.clibeats

import android.app.Application
import dagger.hilt.android.HiltAndroidApp

@HiltAndroidApp
class CliBeatsApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialization routines optimized for fast cold-start (<2s budget)
    }
}
```
Verify zero blocking IO calls exist on main thread startup.
</action>

<acceptance_criteria>
- `CliBeatsApplication.kt` contains zero blocking thread initializers
- `./gradlew assembleDebug` exits 0
</acceptance_criteria>

## Verification
```bash
./gradlew assembleDebug
```
- BUILD SUCCESSFUL (exit 0)
