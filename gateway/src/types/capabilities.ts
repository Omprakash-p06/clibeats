export interface ProviderCapabilities {
  search: boolean;
  playback: boolean;
  playlists: boolean;
  albums: boolean;
  artists: boolean;
  recommendations: boolean;
  radio: boolean;
  downloads: boolean;
  lyrics: boolean;
}
